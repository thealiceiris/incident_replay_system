# W4 Checkov Pre-Lab Prediction (P3)

<!--
Skeleton for cohort-7 W4. Copy (`make checkov-plan-init`) to docs/security/checkov-wk04-plan.md in
your capstone repo.

This is your PRE-LAB PREDICTION (pre-read P3) — NOT the actual scan output. The real scan output goes
in docs/security/checkov-wk04.md during Thu lab Part C; you'll compare the two. The gap between what
you predicted and what Checkov actually flags IS the learning.

Read Checkov Getting Started + the 2-3 rules that apply to your chosen cloud's K8s resource.
-->

**Learner:** <your name>
**Chosen cloud:** <AWS | GCP>

## Rules I expect my Part-A module to FAIL (prediction)

<!--
2-3 rules. Use real Checkov rule IDs for your cloud. Examples:
- CKV_AWS_58 — "EKS cluster has secrets encryption" (AWS)
- CKV_AWS_39 — "EKS cluster endpoint public access is restricted" (AWS)
- CKV_GCP_64 — "GKE cluster uses private nodes" (GCP)
- CKV_GCP_65 — "Manage K8s RBAC users with Google Groups for GKE" (GCP)
Look yours up: https://www.checkov.io/5.Policy%20Index/terraform.html
-->

1. **<RULE_ID>** — <what it checks>. Why it's a reasonable default to enforce: <one sentence>.
2. **<RULE_ID>** — <what it checks>. Why it's a reasonable default to enforce: <one sentence>.
3. **<RULE_ID (optional)>** — <what it checks>. Why a reasonable default: <one sentence>.

## After the scan (Thu Part C)

<!-- Fill the comparison in docs/security/checkov-wk04.md, not here: which predictions were right?
which findings surprised you? which did you fix vs consciously defer (with rationale)? -->
