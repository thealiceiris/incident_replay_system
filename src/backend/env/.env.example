# Local PostgreSQL (docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/incident_replay

# GCP Cloud SQL (when deployed)
# DATABASE_URL=postgresql://postgres:<PASSWORD>@/<DB_NAME>?host=/cloudsql/<PROJECT>:<REGION>:<INSTANCE>

# FastAPI
ENVIRONMENT=development
DEBUG=true
